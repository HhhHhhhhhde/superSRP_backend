<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.superrps.dao.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.superrps.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="last_login_at" property="lastLoginAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, username, password, email, avatar, created_at, last_login_at
    </sql>

    <!-- 根据用户名查询 -->
    <select id="findByUsername" parameterType="string" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE username = #{username}
    </select>

    <!-- 根据ID查询 -->
    <select id="findById" parameterType="long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.example.superrps.entity.User" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, avatar, created_at, last_login_at)
        VALUES (#{username}, #{password}, #{email}, #{avatar}, #{createdAt}, #{lastLoginAt})
    </insert>

    <!-- 更新最后登录时间 -->
    <update id="updateLastLoginTime" parameterType="long">
        UPDATE users
        SET last_login_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.example.superrps.entity.User">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>

